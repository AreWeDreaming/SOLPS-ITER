name: solps_env
channels:
  - conda-forge
dependencies:
  # json
  - json-fortran

  # NCAR Command language
  - ncl
  
  # Motif
  - openmotif-dev

  # Python
  - python
  - netCDF4
  - netcdf-fortran
  - numpy
  - scipy
  - matplotlib

  # Docs
  - pandoc
  
